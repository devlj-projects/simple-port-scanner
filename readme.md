# Simple Port Scanner 
Built to try and learn more about Sockets (module). 

Rudimentary port scanner made with the Socket module. Can scan specified ports or a default list. 
Currently only returns if port is open or closed. 
